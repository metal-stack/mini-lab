auto lo
iface lo inet loopback

# This is here only for Vagrant to access the box. In real world this interface equals lan0.
auto eth0
iface eth0 inet dhcp
    alias Connects (via NAT) to Vagrant host

# In real world this interface is not visible because it is a sandboxed system (BMC)
auto ipmi
iface ipmi inet dhcp

# Ensure a route from mgmt server via firewall to spine exists.
# This route would be added by option routers, but Vagrant overrides it.
# In real world there is a default route to route traffic to spines via fireall.
# In Vagrant case we need to add a static route because the default route is managed by Vagrant.
auto lan0
iface lan0 inet dhcp
    alias Connects to mgmt-firewall
